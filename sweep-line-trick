sort(events.begin(), events.end());
int active = 0;
int start = -1;
for (int i = 0; i < events.size();)
{
    int x = events[i].first;
    // A new merged interval begins.
    if (active == 0)
        start = x;
    // Process all events at coordinate x.
    while (i < events.size() && events[i].first == x)
    {
        active += events[i].second;
        i++;
    }
    // The merged interval ends.
    if (active == 0)
    {
        int end = x - 1;
// for inclusive [l,r] intervals // merged interval = [start, end]
 } 
}
